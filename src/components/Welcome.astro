---
// Aseguramos que no haya código o imports de Astro sobrantes aquí
---

<main>
    <section id="hero">
        
        <!-- Título principal -->
        <h1>
            Arte que Habita en tu Piel
        </h1>
        <p class="description">
            En Cuervo Rosa Studio, transformamos ideas en arte corporal. 
            Fusionamos diseño, técnica y seguridad para crear piezas únicas que 
            cuentan tu historia.
        </p>

        <!-- Botones de acción -->
        <section id="links">
            <a class="button primary" href="#">Agenda tu Cita Ahora</a>
            <a class="button secondary" href="#">Ver Portafolio</a>
        </section>

    </section>
</main>

<style>
    /* Estilos del contenido principal que debe ser visible */
    /* El valor de --navbar-height viene de Layout.astro */
    main {
        min-height: calc(100vh - var(--navbar-height, 4.5rem)); /* Usa espacio restante */
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 2rem;
    }

    #hero {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        max-width: 800px;
    }

    h1 {
        font-size: 3rem; 
        font-weight: 700;
        margin-bottom: 0.5rem;
        color: white; 
    }
    
    .description {
        font-size: 1.1rem;
        color: #ddd; 
        line-height: 1.6;
        margin-bottom: 2rem;
    }

    #links {
        display: flex;
        gap: 1.5rem;
        margin-top: 1rem;
    }

    /* Estilo Base del Botón */
    .button {
        padding: 1rem 2rem;
        border-radius: 12px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    /* Botón Rosa (Agenda tu Cita Ahora) */
    .primary {
        color: white;
        background: linear-gradient(90deg, #FF3366 0%, #E91E63 100%);
    }

    .primary:hover {
        opacity: 0.9;
    }

    /* Botón Blanco (Ver Portafolio) */
    .secondary {
        color: #111827;
        background-color: white;
        border: 2px solid white;
    }
    
    .secondary:hover {
        background-color: #f0f0f0;
    }

    /* Responsive */
    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem;
        }
        #links {
            flex-direction: column;
            width: 100%;
        }
        .button {
            width: 100%;
        }
    }
</style>
