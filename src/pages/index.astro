---
// 1. Importar el componente de la página de bienvenida (el contenido centrado)
import Welcome from '../components/Welcome.astro'; 

// 2. Importar la plantilla base (que ya incluye el Navbar y el fondo oscuro)
import Layout from '../layouts/Layout.astro';
---

<!-- 3. Usar el Layout como contenedor principal.
     El error se debe a que Layout exige el atributo 'title', así que lo agregamos. -->
<Layout title="Cuervo Rosa Studio - Arte en tu Piel">
    <!-- 4. Colocar el componente Welcome dentro del Layout -->
    <Welcome />
</Layout>